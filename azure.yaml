# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: pinecone-canopy
metadata:
    template: pinecone-canopy
services:
    pinecone-canopy:
        project: .
        host: containerapp
        language: python
        docker:
            path: Dockerfile
